# Инструкции для продолжения работы на другом устройстве

На этом устройстве мы обновили систему экспорта смет и разобрались со структурой проекта. Вот текущий статус и то, что нужно сделать дальше.

## ✅ Что было сделано
1. **Синхронизация шаблона**: Обновлен файл `backend/app/services/docx/template.docx` — теперь он идентичен вашему новому шаблону из корня проекта.
2. **Проверка логики**: Код в `expenses.py` проверен, он корректно передает данные в шаблон (sender_name, items, total_amount и т.д.).
3. **Анализ папок**: Выяснили, что папка `_obsolete` — это архив старых данных, а `@expence-tracher-pro` — вероятно, опечатка в отображении названия проекта.

## 🚀 TODO: Что нужно сделать на новом устройстве

### 1. Первичная настройка
- Убедитесь, что установлены все зависимости:
  - Backend: `pip install -r backend/requirements.txt` (важно наличие `docxtpl`, `pandas`, `openpyxl`).
  - Frontend: `pnpm install` или `npm install`.

### 2. Применение изменений (если используете Docker)
Если проект запущен через Docker, обязательно пересоберите образы, чтобы новый шаблон попал в контейнер:
```bash
docker-compose up --build
```

### 3. Проверка экспорта
1. Зайдите в панель администратора.
2. Откройте любую заявку (ExpenseDetail).
3. Нажмите кнопку **"Смета"**.
4. Убедитесь, что скачанный файл содержит данные, а не пустые поля.

### 4. Сопоставление полей в шаблоне
Если данные всё еще не отображаются, проверьте, что в вашем `.docx` файле используются именно эти названия переменных:
- `{{ sender_name }}` — ФИО ответственного
- `{{ sender_position }}` — Должность
- `{{ purpose }}` — Цель/проект
- `{{ date }}` — Дата
- `{{ request_id }}` — ID запроса
- `{{ total_amount }}` — Итоговая сумма
- `{{ currency }}` — Валюта
- Цикл для товаров: `{% for item in items %}`
  - `{{ item.no }}` — №
  - `{{ item.name }}` — Название
  - `{{ item.quantity }}` — Кол-во
  - `{{ item.price }}` — Цена
  - `{{ item.total }}` — Сумма по строке
- `{% endfor %}`

### 5. Очистка (по желанию)
- Проверьте папку `_obsolete`. Если старые схемы БД и история Git за 2024-2025 год вам не нужны, её можно удалить для экономии места.

### 6. Переменные окружения
- Не забудьте скопировать `.env` файлы (они обычно в `.gitignore`), если они не синхронизируются автоматически.
