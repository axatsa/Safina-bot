# API URL for the backend
# In production, this can be omitted if proxied via /api
VITE_API_URL=/api

# Base URL for the web form (used by the bot to generate links)
# VITE_WEB_FORM_URL=http://localhost:8081
