<<<<<<< HEAD
# DO NOT EDIT - SYSTEM FIXED VERSION
# This file is optimized for the server's specific Docker and GitLab Runner configuration.

stages:
  - 🔨 build
  - 🧪 test
  - 🚀 deploy

variables:
  BACKEND_IMAGE: $CI_REGISTRY_IMAGE/backend:$CI_COMMIT_REF_NAME
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: ""

# ========================================
# BUILD STAGE
# ========================================
build-backend:
  stage: 🔨 build
  image: docker:latest
  services:
    - docker:dind
=======
stages:
  - 🔨 build
  - 🧪 test  
  - 🚀 deploy

variables:
  BACKEND_IMAGE: $CI_REGISTRY_IMAGE/backend:$CI_COMMIT_REF_NAME
  DOCKER_DRIVER: overlay2

# ========================================
# BUILD STAGE - Build & Push Docker Image
# ========================================
build-backend:
  stage: 🔨 build
  image: docker:24-dind
  services:
    - docker:24-dind
>>>>>>> f665100 (Fix: GitLab CI/CD pipeline and docker-compose registry images)
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - echo "Building backend image..."
<<<<<<< HEAD
    - docker build -t $BACKEND_IMAGE ./
=======
    - docker build -t $BACKEND_IMAGE ./backend
>>>>>>> f665100 (Fix: GitLab CI/CD pipeline and docker-compose registry images)
    - echo "Pushing backend image to registry..."
    - docker push $BACKEND_IMAGE
  only:
    - main
<<<<<<< HEAD

# ========================================
# TEST STAGE
# ========================================
test-backend:
  stage: 🧪 test
  image: python:3.12-slim
  script:
    - echo "Validating backend environment..."
    - python --version
  only:
    - main

# ========================================
# DEPLOY STAGE
=======
  tags:
    - docker

# ========================================
# DEPLOY STAGE - Deploy to Server
>>>>>>> f665100 (Fix: GitLab CI/CD pipeline and docker-compose registry images)
# ========================================
deploy-backend:
  stage: 🚀 deploy
  image: alpine:3.20
  before_script:
    - apk add --no-cache openssh-client
    - mkdir -p ~/.ssh
    - echo "$SSH_KEY" | tr -d '\r' > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts
  script:
    - |
      ssh -o StrictHostKeyChecking=no -p $SSH_PORT $SSH_USER@$SSH_HOST "
        cd /home/finance/backend/main &&
        docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY &&
<<<<<<< HEAD
        echo 'Pulling...' &&
        docker-compose pull app &&
        echo 'Restarting specific container with --no-deps to bypass server bug...' &&
        docker stop finance-backend-main || true &&
        docker rm finance-backend-main || true &&
        docker-compose up -d --no-deps app
=======
        docker-compose pull backend &&
        docker-compose up -d backend
>>>>>>> f665100 (Fix: GitLab CI/CD pipeline and docker-compose registry images)
      "
  only:
    - main
  dependencies:
    - build-backend
<<<<<<< HEAD
=======
  tags:
    - docker
>>>>>>> f665100 (Fix: GitLab CI/CD pipeline and docker-compose registry images)
